/**
* @author GuangHui
* @description gen-request 单元测试
*/

const BaGenRequest = require('../lib')

describe('BaGenRequest', () => {
  it('should be a function', () => {
    expect(typeof BaGenRequest === 'function').toBe(true)
  })
})
