import { Meta } from '@storybook/addon-docs/blocks'
import svgList from '../src'

import SvgTable from './svg-table'

<Meta
  title="Assets/BaSvgAssets"
  parameters={{ previewTabs: { canvas: { hidden: true } }, docsOnly: true }}
/>

# @ba/svg-assets

## Install

```bash
npm i @ba/svg-assets --registry=https://artifacts.iflytek.com/artifactory/api/npm/npm-repo/

or

yarn add @ba/svg-assets --registry=https://artifacts.iflytek.com/artifactory/api/npm/npm-repo/
```

## Usage

```js
// 获取所有资源列表
import AssetsList from '@ba/svg-assets'

// 导入部分资源(以 svg 示例)
import goTopSvg from '@ba/svg-assets/assets/go-top.svg'

// 批量导入
const requireAll = (requireContext) => requireContext.keys().map(requireContext)
requireAll(require.context('@ba/svg-assets/assets', false, /\\.svg$/))
```

## List

<SvgTable />
